import { logger } from '../utils/logger'

logger.info('Starting HIDEYOU PRO Telegram Bot...')
logger.info(`Mode: ${process.env.NODE_ENV || 'production'}`)

// Bot will be implemented when full bot code is ported
logger.info('Bot placeholder — waiting for full implementation')
